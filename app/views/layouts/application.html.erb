<!DOCTYPE html>
<html>
  <head>
    <title>devitary</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/inter-ui@3.12.1/inter.css" type="text/css" media="all" />
    <link rel="icon" href="<%= image_path 'logo.png' %>" type="image/png" />
    <%= csp_meta_tag %>
    <%= csrf_meta_tags %>

    <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1"/>
    <%= stylesheet_pack_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body class="min-h-screen font-display bg-gray-100 overflow-x-hidden">
    <%= render partial: "partials/header.html" %>
    <%= yield %>
  </body>
</html>
